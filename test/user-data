#cloud-config
version: v1
packages_update: true
packages_upgrade: true

# if you're already installing other packages, you may
# wish to manually install ansible to avoid multiple calls
# to your package manager
runcmd:
 - /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/JulI0-concerto/latitude_manager/v0.0.2/scripts/init_validator.sh)" -c testnet -r 200 -s 50 -v v0.0.2
